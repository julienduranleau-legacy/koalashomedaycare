;(function($) {

    /*var currentBoldTitle = 1

    $('.headerTitle' + currentBoldTitle).css('font-family', 'bariolregular_bold');

    setInterval(alternateBoldTitle, 3000)

    function alternateBoldTitle() {
        var oldBoldTitle = currentBoldTitle
        
        currentBoldTitle++
        if (currentBoldTitle === 4) currentBoldTitle = 1

        $('.headerTitle' + oldBoldTitle).css('font-family', 'bariol_regularregular');
        $('.headerTitle' + currentBoldTitle).css('font-family', 'bariol_regularregular');
        
        setTimeout(function(){ 
            $('.headerTitle' + oldBoldTitle).css('font-family', 'bariolregular_light');
            $('.headerTitle' + currentBoldTitle).css('font-family', 'bariolregular_bold');
        }, 20)
    }*/

})(jQuery);